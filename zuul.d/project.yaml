- project:
    templates:
      - openstack-python-jobs
      - openstack-python35-jobs
      - check-requirements
      - release-notes-jobs
    check:
      jobs:
        - openstack-tox-lower-constraints
        - openstack-tox-cover:
            voting: false

    gate:
      jobs:
        - openstack-tox-lower-constraints
